var app = require('../app');
var http = require('http');
var port =  '3000';
app.set('port', port);

//Create HTTP server
var server = http.createServer(app);
server.listen(port);
console.log("open the link in 127.0.0.1:3000");